# Comma-separated origins; set your deployed HTTPS origin in production.
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
APP_ENV=development
MODEL_PATH=credit_score_model.pkl
MODEL_VERSION=legacy-demo-model
